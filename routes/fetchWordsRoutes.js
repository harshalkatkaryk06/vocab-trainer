// import express from 'express';
// import fetchWordsController from '../controllers/fetchWords.js';
// import { isAuth } from '../middlewares/isAuth.js';

// const router = express.Router();

// // Use isAuth middleware to protect the route
// router.get('/', isAuth, fetchWordsController);

// export default router;
